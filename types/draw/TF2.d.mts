/** @summary draw TF2 object
  * @desc TF2 always drawn via temporary TH2 object,
  * therefore there is no special painter class
  * @private */
export function drawTF2(dom: any, func: any, opt: any): Promise<any>;
